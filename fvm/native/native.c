#include <stdio.h>
#include <stdlib.h>
#include <dlfcn.h> /** For dlopen(), dlsym(), and dlerror()s **/
typedef void* arbita
void* load_native_library(char* name)
{
	void *handle;
	
}
uint32_t native_call(uint32_t* Stack, uint32_t numberofargs)
{
	
}
